#include <bits/stdc++.h>
#define rep(i, N) for (int i = 0; i < (N); i++)
using namespace std;

int main() {
    string S;
    cin >> S;

    rep(i, S.size()) {
        cout << 'x';
    }
    cout << endl;

    return 0;
}
