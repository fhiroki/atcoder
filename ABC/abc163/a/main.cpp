#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
typedef long long ll;
using namespace std;

int main() {
    double r;
    cin >> r;
    double pi = 3.14159265;

    double ans = 2.0 * pi * r;

    cout << ans << endl;
    return 0;
}
