#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
typedef long long ll;
using namespace std;

int main() {
    int r, g;
    cin >> r >> g;

    int ans = 2 * g - r;

    cout << ans << endl;
    return 0;
}
