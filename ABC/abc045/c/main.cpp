#include <bits/stdc++.h>
#define rep(i, N) for (int i = 0; i < (N); i++)
using namespace std;
using ll = long long;

int main() {
    string S;
    cin >> S;

    int ans = 0;

    cout << ans << endl;
    return 0;
}
