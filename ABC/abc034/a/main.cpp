#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
typedef long long ll;
using namespace std;

int main() {
    int x, y;
    cin >> x >> y;

    if (y > x)
        cout << "Better" << endl;
    else
        cout << "Worse" << endl;
    return 0;
}
