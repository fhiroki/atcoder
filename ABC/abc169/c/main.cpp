#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
typedef long long ll;
using namespace std;

int main() {
    ll a;
    long double b;
    cin >> a >> b;

    ll ans = floor(a * b);

    printf("%lld\n", ans);
    return 0;
}
