#include <bits/stdc++.h>
#define rep(i, N) for (int i = 0; i < (N); i++)
using namespace std;

int main() {
    float N;
    cin >> N;

    int ans = ceil(N / 2);

    cout << ans << endl;
    return 0;
}
