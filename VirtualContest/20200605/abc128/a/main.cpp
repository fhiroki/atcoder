#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
typedef long long ll;
using namespace std;

int main() {
    int a, p;
    cin >> a >> p;

    int ans = (a * 3 + p) / 2;

    cout << ans << endl;
    return 0;
}
